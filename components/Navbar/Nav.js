
const Nav = ({ children }) => {
    return (
        <nav className="md:max-w-screen-md w-full m-auto h-full px-3 flex items-center">
            {children}
        </nav>
    )
}

export default Nav
